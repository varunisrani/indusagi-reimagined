'use client';
import { useState } from 'react';
import { Terminal } from 'lucide-react';
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
import { CopyButton } from './copy-button';
export function Installer(){const[product,setProduct]=useState('framework');const[lang,setLang]=useState('npm');const commands:Record<string,Record<string,string>>={framework:{npm:'npm install -g indusagi',pip:'pip install indusagi',cargo:'cargo install indusagi'},agent:{npm:'npm install -g indusagi-coding-agent',pip:'pip install induscode',cargo:'cargo install induscode'}};return <div className="installer" id="install"><div className="installer-label"><Terminal size={18}/><span>Start building in one command</span></div><Tabs value={product} onValueChange={setProduct}><TabsList className="product-tabs" aria-label="Product"><TabsTrigger value="framework">indusagi <span>Framework</span></TabsTrigger><TabsTrigger value="agent">induscode <span>Coding agent</span></TabsTrigger></TabsList>{['framework','agent'].map(p=><TabsContent key={p} value={p}><Tabs value={lang} onValueChange={setLang}><TabsList className="language-tabs" variant="line" aria-label="Package manager"><TabsTrigger value="npm">npm <span>(node)</span></TabsTrigger><TabsTrigger value="pip">pip <span>(python)</span></TabsTrigger><TabsTrigger value="cargo">cargo <span>(rust)</span></TabsTrigger></TabsList>{['npm','pip','cargo'].map(l=><TabsContent key={l} value={l}><div className="command-row"><code><span>$</span> {commands[p][l]}</code><CopyButton text={commands[p][l]}/></div></TabsContent>)}</Tabs></TabsContent>)}</Tabs><p className="requirements">Requires Node 20+ (npm), Python 3.11+ (pip), or Rust 1.96+ (cargo). Set a provider key, then run.</p></div>}
